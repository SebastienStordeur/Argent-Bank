import React from "react";
import HeroContent from "./HeroContent";

const Hero: React.FC = () => {
  return (
    <div className="hero">
      <HeroContent />
    </div>
  );
};

export default Hero;
